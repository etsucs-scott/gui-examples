@page "/"

<PageTitle>Home</PageTitle>

<h1>Solar System Demo</h1>

<p>
    This Blazor app reads planet data from <code>SolarSystem.Core</code>.
    The shared class library owns the model and the in-memory service, while this project handles only UI.
</p>

<p>
    Use the navigation menu to open the planet browser and inspect the same domain data that also powers the API + HTMX example.
</p>
